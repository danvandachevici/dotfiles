alias python=python3
alias pip=pip3

alias ta="tmux a -t"
alias tc="tmux new-session -A -s "
alias tl="tmux ls"

alias vim="vim -p"

alias pingmain="ping mainserver"
alias k="kubectl"
alias klist="kubectl get pods -n"
